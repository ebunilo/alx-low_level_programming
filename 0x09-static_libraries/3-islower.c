#include "main.h"

/**
 * _islower - prints 1 or depending on input
 * @c: first parameter
 *
 * Destination: prints all lowercase letters
 * Return: Always (0)
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
