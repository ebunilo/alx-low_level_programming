#include <stdio.h>

/**
 * _strcmp - Compare two strings
 * @s1: string 1
 * @s2: string 2
 *
 * Return: Always (0) Success
 */

int _strcmp(char *s1, char *s2)
{
	int i = 0;

	while (str1[i])
		if (str1[i] == str2[i])
			return 0;

	return (1);
}
