#include <stdio.h>

/**
 * _strlen - retuns the length of a string
 * @str: the string to count its length
 *
 * Return: int length

int _strlen(char *s)
{
	int i = 1;

	while (str[i] != '\0')
	{
		i++
	}

	return (i)
}
