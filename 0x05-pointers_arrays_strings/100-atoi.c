#include <stdio.h>

/**
 * _atoi - Convert string to integer
 * @s: parameter
 *
 * Return: integer or 0 if there are no numbers
 */

int _atoi(char *s)
{
	int i, valid = 1;

	while (valid)
	{
		if (s[i] == "-" && s[i + 1] 
	}
}
