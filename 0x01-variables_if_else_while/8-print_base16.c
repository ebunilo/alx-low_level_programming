#include <stdio.h>

/**
 * main - Entry point
 * @void: does not take any paramenter
 *
 * Return: Always (0)
 */

int main(void)
{
	int i;

	for (i = '1'; i <= 'f'; i++)
		putchar(i);
	putchar('\n');

	return (0);
}
