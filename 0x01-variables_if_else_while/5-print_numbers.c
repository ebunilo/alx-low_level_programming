#include <stdio.h>

/**
 * main - Entry point
 * @void: does not take any parameter
 *
 * Return: Always (0)
 */

int main(void)
{
	int i = 0;
	for (i = 0; i < 10; i++)
		putchar(i);
	putchar('\n');

	return (0);
}
