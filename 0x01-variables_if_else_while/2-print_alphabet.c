#include <stdio.h>

/**
 * main - Entry point to the program
 * @void: does not accept any parameter
 *
 * Return: Always (0)
 */

int main(void)
{
	char c;

	for (c = a; c <= z; c++)
		putchar(c);
	putchar('\n');
}
